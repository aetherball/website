import { GamesDropdown } from "./games-dropdown";

export function NavigationLinks() {
  // Focus on CTA, calling out the options the users have (and why they should click on them) for the game

  // TODO: Should collapse into hamburger menu + trigger on mobile
  // TODO: actually include subpath links down the line
  return <GamesDropdown />;
}
